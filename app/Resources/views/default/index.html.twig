{% extends 'base.html.twig' %}

{% block meta %}
  <meta name="description" content="">
  <meta name="keywords" content="">
  <meta name="author" content="Seeker225">
{% endblock %}

{% block body %}
    <!-- Carroussel -->
    <div id="myCarousel" class="carousel slide" data-ride="carousel">
        <ol class="carousel-indicators">
            {% for nb in 0..(sliders|length|number_format - 1) %}

                {% if nb == 0 %}
                    <li data-target="#myCarousel" data-slide-to="{{ nb }}" class="active"></li>
                {% else %}
                    <li data-target="#myCarousel" data-slide-to="{{ nb }}"></li>
                {% endif %}

            {% endfor %}
        </ol>

        <div class="carousel-inner" role="listbox">

            {% for slider in sliders %}

                {% if slider.altImg == "Bienvenue à l'USCAEP" %}

                    <div class="carousel-item active">
                        <img class="first-slide" src="{{ asset("frontend/img/bg-1900x1080.png") }}" alt="{{ slider.altImg }}" />
                            <div class="container">
                                <div class="carousel-caption text-left">
                                    <h1>{{ slider.altImg }}</h1>
                                    <p>Nous sommes une union de coopératives ayant pour finalité l’amélioration des conditions de vie des producteurs et de leur environnement.</p>
                                    <p><a class="btn bg-blue" href="{{ path('about') }}" role="button">Voir plus</a></p>
                                </div>
                            </div>
                        </div>

                {% endif %}

                {% if slider.altImg == "Le monde du café" %}

                    <div class="carousel-item">
                        <img class="second-slide" src="{{ slider.webPath }}" alt="{{ slider.altImg }}" />
                        <div class="container">
                            <div class="carousel-caption text-left">
                                <h1>Découvrez le café !</h1>
                                <p>Une tasse de café, le matin, c'est du bonheur. Toutefois, découvrir ce qui se fait avant d'avoir le produit fini peut s'avérer très fructueux.</p>
                                <p><a class="btn bg-orange" href="{{ path('details_culture', {'culture': "café"}) }}" role="button">Voir plus</a></p>
                            </div>
                        </div>
                    </div>

                {% endif %}

                {% if slider.altImg == "Du bon cacao chez l'USCAEP" %}
                  
                    <div class="carousel-item">
                        <img class="second-slide" src="{{ slider.webPath }}" alt="{{ slider.altImg }}" />
                        <div class="container">
                            <div class="carousel-caption text-left">
                                <h1>{{ slider.altImg }}</h1>
                                <p>Bénéficiant d'une main d'oeuvre originaire de la Côte d'Ivoire et de qualité, notre cacao vous met un cran au dessus des autres exportateurs.</p>
                                <p><a class="btn bg-orange" href="{{ path('details_culture', {'culture': "cacao"}) }}" role="button">Voir plus</a></p>
                            </div>
                        </div>
                    </div>

                {% endif %}

                {% if slider.altImg == "pub" %}
                  
                    <div class="carousel-item">
                        <img class="second-slide" src="{{ slider.webPath }}" alt="{{ slider.altImg }}" />
                        <div class="container">
                            <div class="carousel-caption text-left">
                                <h1>Faites de bonnes affaires !</h1>
                                <p>Les cultures de rente, des légumes et d'autres produits sont disponibles dans notre espace publicitaire.</p>
                                <p><a class="btn bg-green" href="{{ path('boutique') }}" role="button">Consulter</a></p>
                            </div>
                        </div>
                    </div>

                {% endif %}
            
            {% endfor %}

        </div>

        <a class="carousel-control-prev" href="#myCarousel" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Précédent</span>
        </a>
        <a class="carousel-control-next" href="#myCarousel" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Suivant</span>
        </a>

    </div>


    {% for message in app.session.flashbag.get('success') %}
        <div class="alert alert-success col-md-12">
            <button type="button" class="close" data-dismiss="alert" arial-label="Fermer">
                <span aria-hidden="true">&times;</span>
            </button>
            
            {% autoescape false %}
                <p><i class="fa fa-check"></i> {{ message }}</p>
            {% endautoescape %}
            
        </div>
    {% endfor %}

    {% for message in app.session.flashbag.get('new-ach') %}
        <div class="alert alert-success col-md-12">

            <button type="button" class="close" data-dismiss="alert" arial-label="Fermer">
                <span aria-hidden="true">&times;</span>
            </button>
            
            {% autoescape false %}
                <p><i class="fa fa-check"></i> {{ message }}</p>
            {% endautoescape %}
            
        </div>
    {% endfor %}
    <br>

    <!-- Mot du PCA -->
    <div class="row position-relative overflow-hidden p-2 p-md-2 m-md-1 text-center">

        <div class="col-md-4">
            <a href="#">
              <img class="img-fluid rounded mb-3 mb-md-0" src="/USCAEP/web/frontend/img/750x450/uscaep-750x450.png" alt="Logo USCAEP">
            </a>
        </div>
        <div class="col-md-7">
            <h3>
                Mot du Président du Conseil d'Administration
            </h3>
              <p>Lorem ipsum dolor amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur.</p>
        </div>

    </div>
    <hr />

    <div class="d-md-flex flex-md-equal w-100 my-md-1 pl-md-1 p-3 p-md-3 p-lg-3 m-md-1">

        <div class="bg-dark text-white text-center p-1 p-md-1 col-md-3">
            
            <div class="col-md-12 text-center p-3 p-md-3">
                
                <h3>Offres de produits</h3>
                <hr>
                <p>
                    Accédez à notre espace publicitaire et achetez vos produits en ligne et à moindre coût !!<br><br>
                    <a href="{{ path('boutique') }}" class="btn bg-green btn-block">Allez à la boutique</a>
                </p>

            </div>

            <div class="col-md-12 p-3 p-md-3">

                <h3>Présentation</h3>
                <hr>
                <p>
                    L’Union des Sociétés Coopératives Agricoles Entente et Progrès (USCAEP) est une union de coopératives ayant pour objet de regrouper tous les producteurs agricoles [...] 
                    <a href="{{ path('about') }}" class="text-success font-weight-light" >Lire plus</a>
                </p>

            </div>

            <div class="col-md-12 text-center p-3 p-md-3">
                <h3>Espace membre</h3>
                <hr>
                <p> 
                    <strong class="text-danger">Accès restreint aux membres !!</strong>
                    <br><br>
                    <a href="{{ path('login') }}" class="btn bg-orange btn-block">
                        Se connecter
                    </a>
                </p>
            </div>

        </div>

        <div class="mr-md-3 pt-3 px-3 pt-md-5 px-md-5 text-center overflow-hidden">

            <h3>Nos domaines d'activités</h3><hr>
            <div class="row mb-4">
                {% for activite in activites %}
                    <div class="col-md-4 mb-4">
                        <img class="img-fluid rounded mb-3 mb-md-0" alt="{{ activite.altImg }}" src="{{ activite.webPath }}" >
                    </div>
                {% endfor %}

                <div class="col-md-8">
                    <p>
                        Nous travaillent avec des braves producteurs avec pour but ultime la promotion et du développement du secteur agricole en Côte d'Ivoire.
                    </p>
                </div>

                <div class="col-md-4">
                    <a class="btn bg-blue btn-block" href="{{ path('liste_activites') }}">Voir plus</a>
                </div>
            </div>

            <h3> Nos coopératives</h3><hr />
            <div class="row">
                {% for cooperative in cooperatives %}
                    <div class="col-md-3 mb-3">
                        <div class="card h-100 text-center">
                            <a href="{{ path('details_cooperative', {'coop': cooperative.nomCoop}) }}">
                                <i class="fa fa-institution fa-5x text-warning"></i>
                            </a>
                            <br>
                            <div class="card-footer">
                                <a href="{{ path('details_cooperative', {'coop': cooperative.sigleCoop}) }}">
                                {{ cooperative.nomCoop|capitalize }} </a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>

            <h3> Nos partenaires</h3><hr />
            <div class="row">
                {% for partenaire in partenaires %}
                    <div class="col-md-2 mb-2">
                        <div class="card h-100 text-center">
                            <a href="#">
                                <img class="img-fluid mb-3 mb-md-0" alt="{{ partenaire.altImg }}" src="{{ partenaire.webPath }}" >
                            </a>
                        </div>
                    </div>
                {% endfor %}
            </div>

        </div>

    </div>
    
</div>

{% endblock %}
